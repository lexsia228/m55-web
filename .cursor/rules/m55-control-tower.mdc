---
description: M55 control-tower boot — repo-as-memory, cold-start handoff, high-cost rerun prohibition.
alwaysApply: true
---

# M55 Control Tower (always apply)

**Do not treat `.cursorrules` or chat memory as authority. Read `AGENTS.md` first.**

Executable state owner:

`docs/ssot/M55_EXECUTION_STATE.json`

It is the sole executable CURRENT GATE / NEXT authority. `M55_CURRENT_STATE.md` remains narrative/history while the execution-state file says `legacyExecutionFieldsSuperseded=true`.

Before analysis or mutation:

- read `AGENTS.md`;
- read `docs/ssot/M55_EXECUTION_STATE.json`;
- read the required SSOT/operations/evidence files from the AGENTS boot order;
- run `npm run m55:context` after `git fetch origin` when local runtime exists;
- if local runtime does not exist, declare `LOCAL_RUNTIME_UNAVAILABLE` and use fresh connected GitHub/Vercel evidence without inventing local facts;
- preserve `GATE_LOCAL_UNPROVEN != HISTORICALLY_UNPROVEN`;
- ledger search → invalidating dependency compare → otherwise **RERUN_PROHIBITED**;
- do not replay completed development gates;
- during `CONTROL-TOWER-COLD-START-ACCEPTANCE-RERUN`, mutation is prohibited and Pair mapping is not yet authorized.

For user-visible work, also read:

- `docs/ssot/M55_COMMERCIAL_QUALITY_CONTRACT.md`
- `docs/ssot/M55_UX_BENCHMARK_STACK.md`

Use the frozen surface mapping in `M55_UX_BENCHMARK_STACK.md`. Do not perform ad-hoc benchmark research absent a documented invalidator.

Before creating page-local header, footer, legal, support, privacy, or refund navigation, inspect shared ownership in `PublicShell`, `PublicHeaderContainer`, `PublicHeader`, and `PublicFooter`. Do not duplicate site-wide chrome when the shared owner already provides it.

Fresh GPT acceptance contract:

`docs/ssot/M55_GPT_COLD_START_ACCEPTANCE.md`

Any authority conflict or unexplained identity drift is STOP.

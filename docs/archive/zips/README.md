# docs/archive/zips
This folder stores large ZIP artifacts as archives.
ZIP files are intentionally ignored by git to avoid repository bloat and accidental public exposure.

Rule:
- Do NOT unzip here.
- Keep only lightweight manifests/notes under version control.

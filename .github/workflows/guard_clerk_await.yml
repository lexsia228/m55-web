name: guard-clerk-await

on:
  push:
    branches: [main]
  pull_request:

jobs:
  guard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Clerk v6 await 漏れガード
        run: node .github/scripts/guard_clerk_await.mjs

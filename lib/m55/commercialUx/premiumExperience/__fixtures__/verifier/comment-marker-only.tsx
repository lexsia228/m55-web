'use client';
/** Negative fixture: comment contains marker names only. */
export default function CommentMarkerFixture() {
  // PremiumDecisionSurface stateId="premium.share.card"
  return <section data-testid="comment-only">comment marker</section>;
}
